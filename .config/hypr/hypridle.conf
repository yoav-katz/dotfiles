general {
    lock_cmd = pidof hyprlock >/dev/null || hyprlock &
    before_sleep_cmd = loginctl lock-session
    after_sleep_cmd = hyprctl dispatch dpms on
    inhibit_sleep = 3
}

source = ~/.config/hypr/idle.conf
